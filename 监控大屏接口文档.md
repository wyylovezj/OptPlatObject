# 监控大屏数据接口文档

## 1. 文档概述

| 项目 | 说明 |
| --- | --- |
| 文档名称 | 监控大屏（MonitorDashboard）数据接口文档 |
| 适用页面 | 首页 → 监控大屏（双屏轮播：运维监控 / ITSM 流程） |
| 前端调用方 | `src/components/homePage/MonitorDashboard.vue` |
| 前端封装模块 | `src/api/homePage.js`（`getOrderData`、`getAlertLevelData`、`getAlertStatisticData`、`getAlertStatusData`），实时告警列表为组件内 `axios` 直接请求 `/searchData` |
| 后端服务 | Python FastAPI 服务（`app.py`，`uvicorn` 启动，监听 `0.0.0.0:8000`） |
| 数据格式 | 请求体 / 响应体均为 JSON（UTF-8） |
| Content-Type | `application/json` |
| 基地址 | `http://<server_ip>:8000`，其中 `server_ip` 由前端 `window.APP_CONFIG?.SERVER_IP` 注入，默认 `127.0.0.1` |

## 2. 通用约定

### 2.1 通用响应字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `status` | string | 业务状态：`success` 成功 / `fail` 失败 |
| `message` | string | 提示信息（失败时返回，如 `"请求处理失败"`） |

> 除 `status`、`message` 外，各接口携带各自的业务数据字段，见下文各接口「响应参数」表。
> 前端校验规则：`status === 'success'` 才消费业务数据。

### 2.2 HTTP 状态码

| 状态码 | 说明 |
| --- | --- |
| 200 | 请求成功（含业务失败，以 `status` 字段区分） |
| 400 | 参数缺失 / 参数非法（如日期为空、type 无效） |
| 500 | 服务端异常 |

### 2.3 数据来源说明

> 当前后端接口返回**模拟（演示）数据**，除固定编号（工单编号、事件编号）外，数值字段每次请求在区间内随机生成；后续接入真实数据源时，响应结构与字段名保持兼容。

---

## 3. 接口清单

| 序号 | 接口路径 | 请求方式 | 用途 | 调用周期 |
| --- | --- | --- | --- | --- |
| 1 | `/getAlertLevelData` | POST | 今日各级别告警数（严重/重要/一般/普通） | 30 秒 |
| 2 | `/getAlertStatisticData` | GET | 告警中心统计（未处理、今日新增、严重、已关闭汇总） | 30 秒 |
| 3 | `/getAlertStatusData` | POST | 今日告警状态统计（未处理/已分派/已完成）→ 闭环收敛 | 30 秒 |
| 4 | `/getOrderData` | POST | ITSM 五类工单待办明细 + SLA 统计 + 近 7 日趋势 | 30 秒 |
| 5 | `/searchData` | POST | 实时未结告警明细列表 | 15 秒 |

---

## 4. 接口详情

### 4.1 获取告警级别数据 `/getAlertLevelData`

#### 4.1.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `date` | string | 是 | 统计日期，格式 `YYYY-MM-DD`（如 `2026-09-06`） |

#### 4.1.2 请求示例

```json
{ "date": "2026-09-06" }
```

#### 4.1.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `total` | number(int) | 当日告警总数（= critical + important + general + ordinary） |
| `critical` | number(int) | 严重告警数 |
| `important` | number(int) | 重要告警数 |
| `general` | number(int) | 一般告警数 |
| `ordinary` | number(int) | 普通告警数 |
| `status` | string | `success` / `fail` |

#### 4.1.4 响应示例

```json
{
  "total": 178,
  "critical": 23,
  "important": 45,
  "general": 62,
  "ordinary": 48,
  "status": "success"
}
```

#### 4.1.5 大屏用途

- 4 张告警统计卡：严重（红）/ 重要（橙）/ 一般（黄）数值与占比进度条；
- 环形图 `已闭环` 之外不作直接消费（数值另有 `/getAlertStatusData`）。

---

### 4.2 获取告警统计汇总 `/getAlertStatisticData`

#### 4.2.1 请求参数

无（GET，不携带请求体）。

#### 4.2.2 请求示例

```
GET http://<server_ip>:8000/getAlertStatisticData
```

#### 4.2.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `unprocessed` | object | 未处理告警汇总 |
| ├ `totalCount` | number(int) | 未处理总数 |
| ├ `critical` | number(int) | 未处理中严重数 |
| └ `assigned` | number(int) | 未处理中已分派数 |
| `added` | object | 新增告警汇总 |
| ├ `today` | number(int) | 今日新增 |
| ├ `yesterday` | number(int) | 昨日新增 |
| ├ `week` | number(int) | 本周新增 |
| └ `month` | number(int) | 本月新增 |
| `serious` | object | 严重告警汇总 |
| ├ `count` | number(int) | 严重总数 |
| ├ `totalCritical` | number(int) | 严重临界值统计 |
| ├ `recently` | string | 最近一条严重告警时间，格式 `YYYY-MM-DD HH:mm:ss` |
| └ `furthest` | string | 最远一条严重告警时间，格式 `YYYY-MM-DD HH:mm:ss`（当前模拟固定值） |
| `completed` | object | 已关闭告警汇总 |
| ├ `count` | number(int) | 已关闭总数 |
| ├ `average` | number(int) | 平均处理时长（秒） |
| ├ `fastest` | number(int) | 最快处理时长（秒） |
| └ `slowest` | number(int) | 最慢处理时长（秒） |
| `status` | string | `success` / `fail` |

#### 4.2.4 响应示例

```json
{
  "unprocessed": { "totalCount": 57, "critical": 12, "assigned": 23 },
  "added": { "today": 86, "yesterday": 72, "week": 610, "month": 2870 },
  "serious": { "count": 45, "totalCritical": 30, "recently": "2026-09-06 14:32:05", "furthest": "2026-04-01 08:30:05" },
  "completed": { "count": 82, "average": 45213, "fastest": 25, "slowest": 328 },
  "status": "success"
}
```

#### 4.2.5 大屏用途

- `added.today` → 「新增 (Info)」统计卡主数值；
- 其余字段暂由大屏预留（供后续扩展）。

---

### 4.3 获取告警状态数据 `/getAlertStatusData`

#### 4.3.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `date` | string | 是 | 统计日期，格式 `YYYY-MM-DD` |

#### 4.3.2 请求示例

```json
{ "date": "2026-09-06" }
```

#### 4.3.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `total` | number(int) | 当日告警总数（= unprocessed + assigned + completed） |
| `unprocessed` | number(int) | 未处理数（大屏口径：处置中） |
| `assigned` | number(int) | 已分派数 |
| `completed` | number(int) | 已完成数（大屏口径：已闭环） |
| `status` | string | `success` / `fail` |

#### 4.3.4 响应示例

```json
{
  "total": 186,
  "unprocessed": 64,
  "assigned": 41,
  "completed": 81,
  "status": "success"
}
```

#### 4.3.5 大屏用途

- `completed` → 环形图「已闭环」、页脚 `已闭环` 数值；
- `unprocessed` → 环形图「处置中」、页脚 `处置中` 数值；
- 两者均为 0 时该卡槽显示「暂无告警数据」空态动画。

---

### 4.4 获取 ITSM 工单数据 `/getOrderData`

#### 4.4.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 当前登录用户名（取自前端 `sessionStorage['user']`） |

#### 4.4.2 请求示例

```json
{ "username": "weiyangyang" }
```

#### 4.4.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `request` | array[object] | 请求类工单列表（当前 5 条） |
| `publish` | array[object] | 发布类工单列表（当前 4 条） |
| `event` | array[object] | 事件类工单列表（当前 8 条） |
| `change` | array[object] | 变更类工单列表（当前 5 条） |
| `problem` | array[object] | 问题类工单列表（当前 3 条） |
| `sla` | object | SLA 达成率统计 |
| `week_trend` | array[object] | 近 7 日五类工单每日新建数（7 条，date 升序） |
| `sla_trend` | array[object] | 近 7 日 SLA 达成率（7 条，date 升序；首条=今日，与 `sla.rate` 同源） |
| `status` | string | `success` / `fail` |

#### 4.4.4 工单对象字段（request / publish / event / change / problem 数组元素）

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `id` | string | 工单编号（固定，如 `INC2026090601`，防止前端待办去重误报新增） |
| `title` | string | 工单标题 |
| `applicant` | string | 申请人 |
| `status` | string | 处理状态：`审批中` / `处理中` / `发布中` / `实施中` / `已受理` / `待处理` / `待审批` |
| `currentHandler` | string | 当前处理人（未分派时返回 `未分派`） |
| `previousHandler` | string | 上一步处理人（无前置环节时返回 `-`） |
| `createTime` | string | 提交时间，格式 `YYYY-MM-DD HH:mm:ss`（相对当前时间固定偏移） |
| `task_url` | string | 工单跳转链接（演示值，当前为 `https://www.baidu.com`） |

#### 4.4.5 `sla` 对象字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `rate` | number(float) | 达成率（%），保留 2 位小数（= achieved / total × 100） |
| `total` | number(int) | 周期内工单总数（300 ~ 600 随机） |
| `achieved` | number(int) | 达标工单数 |
| `overtime` | number(int) | 超时工单数（0 ~ 5 随机） |
| `baseline` | number(float) | 目标基线（固定 `99.5`） |

#### 4.4.6 `week_trend` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `date` | string | 日期，格式 `MM-DD` |
| `request` | number(int) | 当日请求工单新建数（8 ~ 32 随机） |
| `publish` | number(int) | 当日发布工单新建数（2 ~ 14 随机） |
| `event` | number(int) | 当日事件工单新建数（10 ~ 40 随机） |
| `change` | number(int) | 当日变更工单新建数（3 ~ 16 随机） |
| `problem` | number(int) | 当日问题工单新建数（1 ~ 8 随机） |

#### 4.4.7 `sla_trend` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `date` | string | 日期，格式 `MM-DD` |
| `rate` | number(float) | 当日达成率（%）：首条与 `sla.rate` 一致；历史日 99.00 ~ 100.00 随机 |

#### 4.4.8 响应示例

```json
{
  "request": [
    {
      "id": "REQ2026090601",
      "title": "申请开通生产库只读账号权限",
      "applicant": "钱小雨",
      "status": "审批中",
      "currentHandler": "刘东",
      "previousHandler": "钱小雨",
      "createTime": "2026-09-06 14:32:00",
      "task_url": "https://www.baidu.com"
    }
  ],
  "publish": [],
  "event": [],
  "change": [],
  "problem": [],
  "sla": {
    "rate": 99.61,
    "total": 512,
    "achieved": 510,
    "overtime": 2,
    "baseline": 99.5
  },
  "week_trend": [
    { "date": "08-31", "request": 21, "publish": 9, "event": 26, "change": 7, "problem": 4 }
  ],
  "sla_trend": [
    { "date": "08-31", "rate": 99.43 },
    { "date": "09-06", "rate": 99.61 }
  ],
  "status": "success"
}
```

#### 4.4.9 大屏用途

- 8 张 ITSM 指标卡（SLA 达成率、工单总览、事件/问题/变更/请求/发布管理）数值与最新明细行；
- 第三排图表：工单趋势折线（`week_trend`）、分类占比环形（`week_trend` 五日合计）、SLA 达成趋势（`sla_trend`）；
- 第四排「工单处理明细流」走马灯（五类数组合流，按 `createTime` 倒序，单条明细渲染一张卡片）。
- 首页 ITSM 待办卡与监控大屏共用本接口（编号 `id` 需保持稳定）。

---

### 4.5 获取实时未结告警明细 `/searchData`

#### 4.5.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| （无） | — | — | 可传空对象 `{}`，后端当前不读取请求体参数 |

#### 4.5.2 请求示例

```json
{}
```

#### 4.5.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `data` | array[object] | 告警明细列表（当前固定生成 200 条模拟数据） |
| `message` | string | 提示信息，如 `查询成功` |
| `status` | string | `success` / `fail` |

#### 4.5.4 `data` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `event_id` | string | 告警事件 ID（0 ~ 199） |
| `severity` | string | 级别：`严重` / `重要` / `一般` / `普通`（加权随机：严重少、普通多） |
| `state` | string | 状态：`未处理` / `已分派` / `已挂起` |
| `system_name` | string | 所属系统名称 |
| `category` | string | 告警分类（当前固定 `中间件`） |
| `object` | string | 监控对象名 |
| `ip` | string | 来源 IP |
| `alarm_details` | string | 告警详情描述 |
| `occurrenceTime` | string | 发生时间，格式 `YYYY-MM-DD HH:mm:ss`（最近 6 分钟内随机） |
| `processingTime` | string | 处理时间，格式 `YYYY-MM-DD HH:mm:ss` |
| `Alarm_Handler` | string | 处理人（模拟 `张三`） |
| `alert_remarks` | string | 备注（模拟 `无`） |

#### 4.5.5 响应示例

```json
{
  "data": [
    {
      "event_id": "0",
      "severity": "重要",
      "state": "未处理",
      "system_name": "核心交易系统",
      "category": "中间件",
      "object": "zcddfsjg0",
      "ip": "192.168.100.103",
      "alarm_details": "您所拨打的电话已关机，请稍后再拨，sorry！",
      "occurrenceTime": "2026-09-06 14:31:22",
      "processingTime": "2026-09-06 14:32:05",
      "Alarm_Handler": "张三",
      "alert_remarks": "无"
    }
  ],
  "message": "查询成功",
  "status": "success"
}
```

#### 4.5.6 大屏用途

- 「实时未结告警列表」滚动轨道数据源；
- 前端取 `data` 数组按 `occurrenceTime` 倒序截取前 12 条，映射展示：`severity` → 级别徽标、`system_name` → 来源、`alarm_details` → 详情、`occurrenceTime` → 相对时间（`x m 前` / `x h 前`）；
- 列表为空（`data` 为空数组）时该槽显示「暂无未结告警」空态动画。

---

## 5. 异常响应示例

参数缺失（HTTP 400）：

```json
{ "message": "日期参数不能为空", "status": "fail" }
```

服务端异常（HTTP 500）：

```json
{ "message": "请求处理失败", "status": "fail" }
```

## 6. 变更记录

| 版本 | 日期 | 变更说明 | 维护人 |
| --- | --- | --- | --- |
| v1.0 | 2026-09-06 | 初版：依据监控大屏前端实际调用与后端 `app.py` 实现整理 | 大屏开发组 |
