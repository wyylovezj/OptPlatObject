# 监控大屏数据接口文档

## 1. 文档概述

| 项目 | 说明 |
| --- | --- |
| 文档名称 | 监控大屏（MonitorrDashboard）数据接口文档 |
| 适用页面 | 首页 → 监控大屏（双屏轮播：运维监控 / ITSM 流程） |
| 前端调用方 | `src/components/homePage/MonitorrDashboard.vue`（组件内 `axios` 直连，无独立 API 封装层） |
| 后端服务 | Python Flask 服务（`Monitorr.py` 提供全部接口，由 `rolePermission.py` 的 `create_app()` 通过 `register_Monitorr_routes(app)` 注册；Gunicorn/开发服务器监听 `0.0.0.0:5000`） |
| 数据格式 | 请求体 / 响应体均为 JSON（UTF-8） |
| Content-Type | `application/json` |
| 基地址 | `http://<server_ip>:5000`，其中 `server_ip` 由前端 `window.APP_CONFIG?.RBAC_IP` 注入（含协议与端口，如 `http://127.0.0.1:5000`），默认 `http://127.0.0.1:5000` |

## 2. 通用约定

### 2.1 通用响应字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `status` | string | 业务状态：`success` 成功 / `fail` 失败 |
| `message` | string | 提示信息（失败时返回，如 `"请求处理失败"`、`"日期参数不能为空"`） |

> 除 `status`、`message` 外，各接口携带各自的业务数据字段，见下文各接口「响应参数」表。
> 前端校验规则：`status === 'success'` 才消费业务数据。

### 2.2 HTTP 状态码

| 状态码 | 说明 |
| --- | --- |
| 200 | 请求成功（含业务失败，以 `status` 字段区分） |
| 400 | 参数缺失 / 参数非法（如日期为空） |
| 500 | 服务端异常 |

### 2.3 数据来源说明

> 当前后端接口返回**模拟（演示）数据**：除工单明细（固定编号与标题）外，数值字段每次请求在固定区间内随机生成；后续接入真实数据源时，响应结构与字段名保持兼容。

### 2.4 值类型约定（重要）

矩阵卡数据接口的卡片属性值分两类，前端按模板原样显示或拼接单位：

| 类型 | 说明 | 示例 |
| --- | --- | --- |
| 数值型 | 返回 JSON 数字；单位/百分号由前端模板拼接或 `toLocaleString()` 格式化 | `host.cpu: 68`（模板拼 `%`）、`slb.qps: 24500`（模板 `toLocaleString()`）、`security.rules: 864`（模板拼 ` 条`） |
| 展示型 | 返回**已带单位/格式**的字符串，前端原样显示 | `db.pool: "76.4%"`、`middleware.rocket: "8,420/s"`、`k8s.nodes: "62/64 Ready"` |

---

## 3. 接口清单

| 序号 | 接口路径 | 请求方式 | 用途 | 调用周期 |
| --- | --- | --- | --- | --- |
| 1 | `/getMonitorAlertLevelData` | POST | 当日各级别告警数（严重/重要/一般/普通） | 30 秒（频次推移另按 5 分钟采样） |
| 2 | `/getMonitorAlertStatusData` | POST | 当日告警状态统计（未处理/已分派/已完成）→ 闭环收敛 | 30 秒 |
| 3 | `/getMonitorAlertStatisticData` | GET | 告警中心统计（未处理、新增、严重、已关闭汇总） | 30 秒 |
| 4 | `/getMonitorOrderData` | POST | ITSM 五类工单待办明细 + SLA 统计 + 近 7 日趋势 | 30 秒 |
| 5 | `/searchMonitorrData` | POST | 实时未结告警明细列表 | 30 秒 |
| 6 | `/getMonitorMatrixData` | POST | 监控矩阵卡数据（应用系统/全栈基础设施/阿里云套件 + 顶部 SLA） | 30 秒 |
| 7 | `/getMonitorKnowledgeData` | POST | 知识库五类条目统计（整数条数 + 占比） | 30 秒 |

---

## 4. 接口详情

### 4.1 获取告警级别数据 `/getMonitorAlertLevelData`

#### 4.1.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `date` | string | 是 | 统计日期，格式 `YYYY-MM-DD`（如 `2026-09-07`） |

#### 4.1.2 请求示例

```json
{ "date": "2026-09-07" }
```

#### 4.1.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `total` | number(int) | 当日告警总数（= critical + important + general + ordinary） |
| `critical` | number(int) | 严重告警数（0 ~ 100 随机） |
| `important` | number(int) | 重要告警数（0 ~ 100 随机） |
| `general` | number(int) | 一般告警数（0 ~ 100 随机） |
| `ordinary` | number(int) | 普通告警数（0 ~ 100 随机） |
| `status` | string | `success` / `fail` |

#### 4.1.4 响应示例

```json
{
  "total": 178,
  "critical": 23,
  "important": 45,
  "general": 62,
  "ordinary": 48,
  "status": "success"
}
```

#### 4.1.5 大屏用途

- 「严重 (Critical)/重要 (Major)/一般 (Minor)」统计卡主数值（占比进度条 = 各级别 / total）；
- 「告警动态频次推移」折线图：`recordTrendSnapshot` 每 5 分钟以本接口采样一次，将 `critical/important/general` 写入 5 分钟网格槽位（24 槽 = 最近 2 小时），刷新/跨天时从 `localStorage` 恢复；
- 首屏 30 秒轮询（`fetchRealtimeData`）亦消费本接口刷新统计卡数值。

---

### 4.2 获取告警状态数据 `/getMonitorAlertStatusData`

#### 4.2.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `date` | string | 是 | 统计日期，格式 `YYYY-MM-DD` |

#### 4.2.2 请求示例

```json
{ "date": "2026-09-07" }
```

#### 4.2.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `total` | number(int) | 当日告警总数（= unprocessed + assigned + completed） |
| `unprocessed` | number(int) | 未处理数（0 ~ 100 随机，大屏口径：处置中） |
| `assigned` | number(int) | 已分派数（0 ~ 100 随机） |
| `completed` | number(int) | 已完成数（0 ~ 100 随机，大屏口径：已闭环） |
| `status` | string | `success` / `fail` |

#### 4.2.4 响应示例

```json
{
  "total": 186,
  "unprocessed": 64,
  "assigned": 41,
  "completed": 81,
  "status": "success"
}
```

#### 4.2.5 大屏用途

- `completed` → 环形图「已闭环」、页脚 `已闭环` 数值；
- `unprocessed` → 环形图「处置中」、页脚 `处置中` 数值；
- 两者均为 0 时该卡槽显示「暂无告警数据」空态动画；环形图 3 秒整组重绘（`repaintSnapshotCharts`）。

---

### 4.3 获取告警统计汇总 `/getMonitorAlertStatisticData`

#### 4.3.1 请求参数

无（GET，不携带请求体）。

#### 4.3.2 请求示例

```
GET http://<server_ip>:5000/getMonitorAlertStatisticData
```

#### 4.3.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `unprocessed` | object | 未处理告警汇总 |
| ├ `totalCount` | number(int) | 未处理总数（0 ~ 100 随机） |
| ├ `critical` | number(int) | 未处理中严重数（0 ~ 50 随机） |
| └ `assigned` | number(int) | 未处理中已分派数（0 ~ 50 随机） |
| `added` | object | 新增告警汇总 |
| ├ `today` | number(int) | 今日新增（0 ~ 200 随机） |
| ├ `yesterday` | number(int) | 昨日新增（0 ~ 200 随机） |
| ├ `week` | number(int) | 本周新增（200 ~ 1000 随机） |
| └ `month` | number(int) | 本月新增（1000 ~ 4000 随机） |
| `serious` | object | 严重告警汇总 |
| ├ `count` | number(int) | 严重总数（0 ~ 100 随机） |
| ├ `totalCritical` | number(int) | 严重临界值统计（0 ~ 50 随机） |
| ├ `recently` | string | 最近一条严重告警时间（= 服务当前时间，实时） |
| └ `furthest` | string | 最远一条严重告警时间（当前固定值 `2026-04-01 08:30:05`） |
| `completed` | object | 已关闭告警汇总 |
| ├ `count` | number(int) | 已关闭总数（0 ~ 100 随机） |
| ├ `average` | number(int) | 平均处理时长，秒（3601 ~ 86400 随机） |
| ├ `fastest` | number(int) | 最快处理时长，秒（1 ~ 60 随机） |
| └ `slowest` | number(int) | 最慢处理时长，秒（61 ~ 350 随机） |
| `status` | string | `success` / `fail` |

#### 4.3.4 响应示例

```json
{
  "unprocessed": { "totalCount": 57, "critical": 12, "assigned": 23 },
  "added": { "today": 86, "yesterday": 72, "week": 610, "month": 2870 },
  "serious": { "count": 45, "totalCritical": 30, "recently": "2026-09-07 09:41:32", "furthest": "2026-04-01 08:30:05" },
  "completed": { "count": 82, "average": 45213, "fastest": 25, "slowest": 328 },
  "status": "success"
}
```

#### 4.3.5 大屏用途

- `added.today` → 「新增 (Info)」统计卡主数值；
- 其余字段暂由大屏预留（供后续扩展）。

---

### 4.4 获取 ITSM 工单数据 `/getMonitorOrderData`

#### 4.4.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 当前登录用户名（取自前端 `sessionStorage['user']`） |

#### 4.4.2 请求示例

```json
{ "username": "weiyangyang" }
```

#### 4.4.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `request` | array[object] | 请求类工单列表（固定 5 条） |
| `publish` | array[object] | 发布类工单列表（固定 4 条） |
| `event` | array[object] | 事件类工单列表（固定 8 条） |
| `change` | array[object] | 变更类工单列表（固定 5 条） |
| `problem` | array[object] | 问题类工单列表（固定 3 条） |
| `sla` | object | SLA 达成率统计（每次随机） |
| `week_trend` | array[object] | 近 7 日五类工单每日新建数（7 条，date 升序） |
| `sla_trend` | array[object] | 近 7 日 SLA 达成率（7 条，date 升序；末条 = 今日，与 `sla.rate` 同源） |
| `status` | string | `success` / `fail` |

#### 4.4.4 工单对象字段（request / publish / event / change / problem 数组元素）

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `id` | string | 工单编号（固定，如 `INC2026090601`，防止首页 ITSM 待办去重误报新增） |
| `title` | string | 工单标题 |
| `applicant` | string | 申请人 |
| `status` | string | 处理状态：`审批中` / `处理中` / `发布中` / `实施中` / `已受理` / `待处理` / `待审批` |
| `currentHandler` | string | 当前处理人（未分派时返回 `未分派`） |
| `previousHandler` | string | 上一步处理人（无前置环节时返回 `-`） |
| `createTime` | string | 提交时间，格式 `YYYY-MM-DD HH:mm:ss`（按固定分钟偏移随当前时间漂移，明细流时间保持"近期"） |
| `task_url` | string | 工单跳转链接（演示值，当前为 `https://www.baidu.com`） |

#### 4.4.5 `sla` 对象字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `rate` | number(float) | 达成率（%），保留 2 位小数（= achieved / total × 100） |
| `total` | number(int) | 周期内工单总数（300 ~ 600 随机） |
| `achieved` | number(int) | 达标工单数 |
| `overtime` | number(int) | 超时工单数（0 ~ 5 随机） |
| `baseline` | number(float) | 目标基线（固定 `99.5`） |

#### 4.4.6 `week_trend` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `date` | string | 日期，格式 `MM-DD` |
| `request` | number(int) | 当日请求工单新建数（8 ~ 32 随机） |
| `publish` | number(int) | 当日发布工单新建数（2 ~ 14 随机） |
| `event` | number(int) | 当日事件工单新建数（10 ~ 40 随机） |
| `change` | number(int) | 当日变更工单新建数（3 ~ 16 随机） |
| `problem` | number(int) | 当日问题工单新建数（1 ~ 8 随机） |

#### 4.4.7 `sla_trend` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `date` | string | 日期，格式 `MM-DD` |
| `rate` | number(float) | 当日达成率（%）：今日（末条）与 `sla.rate` 一致；历史日 99.00 ~ 100.00 随机 |

#### 4.4.8 响应示例

```json
{
  "request": [
    {
      "id": "REQ2026090601",
      "title": "申请开通生产库只读账号权限",
      "applicant": "钱小雨",
      "status": "审批中",
      "currentHandler": "刘东",
      "previousHandler": "钱小雨",
      "createTime": "2026-09-07 09:32:00",
      "task_url": "https://www.baidu.com"
    }
  ],
  "publish": [],
  "event": [],
  "change": [],
  "problem": [],
  "sla": {
    "rate": 99.61,
    "total": 512,
    "achieved": 510,
    "overtime": 2,
    "baseline": 99.5
  },
  "week_trend": [
    { "date": "09-01", "request": 21, "publish": 9, "event": 26, "change": 7, "problem": 4 }
  ],
  "sla_trend": [
    { "date": "09-01", "rate": 99.43 },
    { "date": "09-07", "rate": 99.61 }
  ],
  "status": "success"
}
```

#### 4.4.9 大屏用途

- 8 张 ITSM 指标卡（SLA 达成率、工单总览、事件/问题/变更/请求/发布管理）数值与最新明细行；
- 第三排图表：工单趋势折线（`week_trend`）、分类占比条形（`week_trend` 五类合计）、SLA 达成趋势（`sla_trend`，含 `sla.baseline` 参考线）；图表 3 秒整组重绘；
- 第四排「工单处理明细流」走马灯：五类数组合流，按 `createTime` 倒序，每条明细渲染一张卡片（类别色/状态色由前端 `typeLabel/statusColor` 映射）；
- 首页 ITSM 待办卡与监控大屏共用本接口（编号 `id` 需保持稳定）。

---

### 4.5 获取实时未结告警明细 `/searchMonitorrData`

> 注意：路径为 `/searchMonitorrData`（含 `r`），与告警页面历史接口 `/searchData` 不同；本接口由大屏组件直连调用。

#### 4.5.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| （无） | — | — | 可传空对象 `{}`，后端当前不读取请求体参数 |

#### 4.5.2 请求示例

```json
{}
```

#### 4.5.3 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `data` | array[object] | 告警明细列表（每次固定生成 200 条模拟数据） |
| `message` | string | 提示信息，如 `查询成功` |
| `status` | string | `success` / `fail` |

#### 4.5.4 `data` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `event_id` | string | 告警事件 ID（`"0"` ~ `"199"`） |
| `severity` | string | 级别：`严重` / `重要` / `一般` / `普通`（加权随机，权重 10/20/40/30，严重少、普通多） |
| `state` | string | 状态：`未处理` / `已分派` / `已挂起`（随机） |
| `system_name` | string | 所属系统：`新核心` / `内评系统` / `acrm` / `ocrm`（随机） |
| `category` | string | 告警分类（当前固定 `中间件`） |
| `object` | string | 监控对象名（`zcddfsjg0` ~ `zcddfsjg199`） |
| `ip` | string | 来源 IP（固定 `192.168.100.103`） |
| `alarm_details` | string | 告警详情描述（当前固定文案） |
| `occurrenceTime` | string | 发生时间，格式 `YYYY-MM-DD HH:mm:ss`（最近 6 分钟内随机） |
| `processingTime` | string | 处理时间，格式 `YYYY-MM-DD HH:mm:ss`（= 服务当前时间） |
| `Alarm_Handler` | string | 处理人（模拟 `张三`） |
| `alert_remarks` | string | 备注（模拟 `无`） |

#### 4.5.5 响应示例

```json
{
  "data": [
    {
      "event_id": "0",
      "severity": "重要",
      "state": "未处理",
      "system_name": "新核心",
      "category": "中间件",
      "object": "zcddfsjg0",
      "ip": "192.168.100.103",
      "alarm_details": "您所拨打的电话已关机，请稍后再拨，sorry！",
      "occurrenceTime": "2026-09-07 09:40:22",
      "processingTime": "2026-09-07 09:41:05",
      "Alarm_Handler": "张三",
      "alert_remarks": "无"
    }
  ],
  "message": "查询成功",
  "status": "success"
}
```

#### 4.5.6 大屏用途

- 「实时未结告警列表」滚动轨道数据源：前端取 `data` 按 `occurrenceTime` 倒序截取前 12 条，映射 `severity` → 级别徽标、`system_name` → 来源、`alarm_details` → 详情、`occurrenceTime` → 相对时间（`x m 前` / `x h 前`）；行对象跨刷新按 `id`（`来源|时间|详情`）增量合并，滚动由 50ms 本地定时器驱动；
- 列表为空（`data` 为空数组）时该槽显示「暂无未结告警」空态动画。

---

### 4.6 获取监控矩阵卡数据 `/getMonitorMatrixData`

#### 4.6.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| （无） | — | — | 可传空对象 `{}` |

#### 4.6.2 响应结构总览

```json
{
  "topSla": 99.97,
  "infra": { "app": {}, "api": {}, "host": {}, "db": {}, "middleware": {}, "k8s": {}, "docker": {}, "jvm": {} },
  "cloud": { "ecs": {}, "vpc": {}, "slb": {}, "security": {}, "rds": {}, "oss": {} },
  "status": "success"
}
```

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `topSla` | number(float) | 大屏顶部「SLA 综合健康率」（99.95 ~ 99.99 随机，保留 2 位），前端拼 `%` 展示 |
| `infra` | object | 第 1 屏「应用系统与基础设施监控矩阵」8 张卡数据（app/api/host/db/middleware/k8s/jvm 大屏展示，docker 保留结构） |
| `cloud` | object | 第 1 屏「云资源与安全防护监控」6 张卡数据 |
| `status` | string | `success` / `fail` |

#### 4.6.3 `infra.app`（应用系统卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `health` | string | `99.95%` / `99.98%` / `99.99%` / `100%` | 卡头健康率 |
| `total` | int | 158 ~ 172 | 主数值"系统数" |
| `coreRate` | string | `99.99%` / `100%` / `99.98%` | 核心业务系统可用率 |
| `devCount` | string | `104 ~ 118 个` | 开发类系统 |
| `opsCount` | string | `20 ~ 27 个` | 运维类系统 |
| `branchCount` | string | `28 ~ 35 个` | 分/子公司系统 |
| `logRate` | string | `430 ~ 560 K/时` | 日志增速 |

#### 4.6.4 `infra.api`（服务状态监控卡 / API 链路）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `health` | string | 99.95% ~ 99.99%（2 位小数） | 卡头可用率 |
| `total` | int | 415 ~ 448 | 主数值"接口数" |
| `rtt` | int | 32 ~ 46 | 往返耗时（模板拼 ` ms`） |
| `err4xx` | string | 0.12% ~ 0.48%（2 位小数） | 4xx 错误率 |
| `err5xx` | string | 0.003% ~ 0.018%（3 位小数） | 5xx 错误率 |
| `p99` | string | `72 ~ 120 ms` | P99 耗时 |
| `icmpRate` | string | 99.2% ~ 99.9%（1 位小数） | ICMP 响应率 |

#### 4.6.5 `infra.host`（主机监控卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `total` | int | 240 ~ 258 | 主数值"主机数" |
| `health` | string | `99.9%` / `99.8%` / `100%` | 卡头健康率 |
| `offline` | int | 0 ~ 3 | 离线主机数（预留） |
| `cpu` | int | 52 ~ 88 | CPU Avg（模板拼 `%`） |
| `ram` | int | 58 ~ 90 | RAM Avg（模板拼 `%`） |
| `disk` | int | 40 ~ 82 | 磁盘 I/O（模板拼 `%`） |
| `net` | int | 55 ~ 88 | 网络 IO（模板拼 `%`） |

#### 4.6.6 `infra.db`（原生数据库卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `total` | int | 33 ~ 39 | 主数值"实例数" |
| `health` | string | `100% 在线` / `99.9% 在线` | 卡头状态 |
| `pool` | string | 62% ~ 88%（1 位小数） | 连接池占用 |
| `slow` | string | `9 ~ 20 ops` | 慢查询 QPS |
| `replica` | string | `< 0.03s` / `< 0.05s` / `< 0.08s` | 主从延时 |
| `sessions` | string | `1,050 ~ 1,400 个`（千分位） | 活跃会话 |
| `qpsPeak` | string | 8.6K ~ 11.5K（1 位小数） | QPS 峰值 |

#### 4.6.7 `infra.middleware`（中间件节点卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `total` | int | 48 ~ 58 | 卡头 `xx Nodes` |
| `kafkaLag` | int | 1200 ~ 2600 | 主数值（Kafka 积压） |
| `redisHit` | string | 98.6% ~ 99.8%（1 位小数） | Redis 命中率 |
| `rocket` | string | `7,600 ~ 9,800/s`（千分位） | RocketMQ TPS |
| `zk` | string | `108 ~ 150 会话` | ZooKeeper 连接 |
| `es` | string | `Green 正常` | ES 集群状态 |
| `retry` | string | `0 ~ 3 个` | 消息重试队列 |

#### 4.6.8 `infra.k8s`（K8s 集群卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `pods` | int | 1210 ~ 1320 | 主数值"POD 数" |
| `restarts` | int | 4 ~ 14 | 重启次数（预留） |
| `clusters` | string | `3 ~ 5 Clusters` | 卡头集群数 |
| `nodes` | string | `61 ~ 64/64 Ready` | Node 节点 |
| `pending` | string | `0 ~ 3 Pending` | 异常 Pod |
| `hpa` | string | `8 ~ 16次 / 1h` | 弹性伸缩（预留） |
| `cpuPct` | string | 48% ~ 80% | CPU 水位 |
| `memPct` | string | 56% ~ 85% | 内存水位 |
| `etcd` | string | `Green 正常` | ECTD 状态 |

#### 4.6.9 `infra.docker`（容器运行时，保留结构返回，当前大屏模板未消费）

| 参数名 | 类型 | 取值 / 随机区间 |
| --- | --- | --- |
| `total` | int | 3300 ~ 3600 |
| `health` | string | `99.8%` / `99.9%` |
| `stopped` | int | 8 ~ 20 |
| `oom` | string | `0 ~ 2 触发` |
| `scan` | string | `全合规` |
| `healthChk` | string | `0 ~ 3 失败` |
| `logRate` | string | 1.2 ~ 2.6（1 位小数）` GB/h` |

#### 4.6.10 `infra.jvm`（JVM 运行时卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `threads` | int | 8200 ~ 9100 | 主数值"活跃线程" |
| `heap` | string | 58% ~ 84%（1 位小数） | 堆内存分配 |
| `youngGc` | string | `18 ~ 40ms / 次` | Young GC |
| `fullGc` | string | `0 ~ 3次 / 1h` | Full GC 频次 |
| `classes` | string | 19 ~ 25（1 位小数）`K` | 类加载总数 |
| `deadlock` | string | `0 ~ 1 个` | 死锁线程 |
| `jdk` | string | `JDK 17` / `JDK 21` | 卡头版本 |

#### 4.6.11 `cloud.ecs`（ECS 云服务器卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `count` | int | 498 ~ 528 | 主数值"台实例" |
| `health` | string | `99.9% 稳定` / `99.8% 稳定` / `100% 稳定` | 卡头状态 |
| `cpu` | string | 42% ~ 70%（1 位小数） | 平均 CPU |
| `mem` | string | 48% ~ 76%（1 位小数） | 平均内存 |
| `iops` | string | `15.5 ~ 21.5K / 260 ~ 420MB/s` | IOPS / 吞吐 |
| `disk` | string | 38% ~ 60% | 云盘使用率 |
| `ess` | string | `就绪` / `扩缩中` | ESS 伸缩组 |

#### 4.6.12 `cloud.vpc`（VPC 虚拟专网卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `routes` | int | 36 ~ 48 | 主数值"条路由表" |
| `zones` | string | `38 ~ 46 个隔离区` | 卡头隔离区 |
| `vbr` | string | `10G 双活` / `20G 双活` | 高速通道 VBR |
| `nat` | string | 38 ~ 48（1 位小数）` 万 pps` | NAT 网关 |
| `eip` | string | `23 ~ 28/28 ~ 30` | EIP 弹性IP（已用/总额） |
| `vsw` | string | `78 ~ 96 个` | 子网 vSwitch |
| `flowlog` | string | `已开启` | 流日志 |

#### 4.6.13 `cloud.slb`（SLB 负载均衡卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `count` | int | 26 ~ 31 | 主数值"ALB / CLB" |
| `health` | string | `100% 正常` | 卡头状态 |
| `qps` | int | 22600 ~ 26800 | 入口总 QPS（前端 `toLocaleString()` 千分位） |
| `newConn` | string | 11.2 ~ 14.8（1 位小数）`K /s` | 新建连接 |
| `unhealthy` | string | `0 ~ 1 台` | 异常后端 ECS |
| `ssl` | string | `已开启` | SSL 卸载 |
| `healthChk` | string | `全通过` | 健康检查 |

#### 4.6.14 `cloud.security`（安全组与威胁防护卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `rules` | int | 780 ~ 960 | 今日告警（模板拼 ` 条`） |
| `health` | string | `防御中` / `监测中` / `全力防护中` | 卡头状态 |
| `audit` | string | `全合规` | 审计合规（预留） |
| `blocks` | int | 2800 ~ 3600 | 主数值"今日恶意拦截" |
| `ddos` | string | `Clean 4.2 ~ 7.5G` | DDoS 清洗量 |
| `brute` | string | `96 ~ 160 次` | 暴力破解拦截 |
| `vuln` | string | `0 ~ 4 高危` | 高危告警（预留） |
| `policyCount` | string | `1200 ~ 1320 条` | 威胁检测策略 |
| `triageRate` | string | 98.2 ~ 99.3（2 位小数）` %` | 威胁分诊率 |
| `vulnCount` | string | `240 ~ 300 个` | 高危漏洞未修复 |

#### 4.6.15 `cloud.rds`（RDS 专有实例卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `instances` | int | 22 ~ 27 | 主数值"热备集群" |
| `health` | string | `高可用 HA` | 卡头状态 |
| `replica` | string | `< 0.04s` / `< 0.06s` / `< 0.08s` | 只读延时 |
| `storage` | string | 11.5 ~ 13.5（1 位小数）` TB (55~68%)` | 存储空间 |
| `backup` | string | `已完成` | 自动冷备 |
| `cpu` | string | 12% ~ 34% | 平均 CPU |
| `conns` | string | `1,600 ~ 2,100 个`（千分位） | 活跃连接 |

#### 4.6.16 `cloud.oss`（OSS 对象存储卡）

| 参数名 | 类型 | 取值 / 随机区间 | 大屏展示 |
| --- | --- | --- | --- |
| `size` | int | 168 ~ 200 | 主数值"TB 存储" |
| `health` | string | `14 ~ 19 Buckets` | 卡头桶数 |
| `bandwidth` | string | 700 ~ 980 ` Mbps` | 流出带宽 |
| `reqQps` | string | 38 ~ 48（1 位小数）`K` | 请求 QPS |
| `apiRate` | string | 99.998% ~ 99.999%（3 位小数） | API 响应率 |
| `objects` | string | 2.6 ~ 3.2（2 位小数）` 亿` | 对象总数 |
| `dr` | string | `已同步` | 跨域容灾 |

#### 4.6.17 大屏用途

- `topSla` → 大屏顶部「SLA 综合健康率」；
- `infra.app/api/host/db/middleware/k8s/jvm` → 第 1 屏「应用系统与基础设施监控矩阵」7 张卡（`docker` 为保留结构，模板未消费）；
- `cloud.*` → 第 1 屏「云资源与安全防护监控」6 张卡；
- 前端以接口返回值**整组替换** `infraData/cloudData`（本地仅保留同键空壳占位），30 秒轮询实现全卡数据实时刷新。

---

### 4.7 获取知识库条目统计 `/getMonitorKnowledgeData`

#### 4.7.1 请求参数（Body，JSON）

| 参数名 | 参数类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| （无） | — | — | 可传空对象 `{}` |

#### 4.7.2 响应参数

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `total` | number(int) | 知识条目总数（= 五类条数之和） |
| `items` | array[object] | 五类条目统计（固定 5 项，顺序：系统类 → 容器云 → 网络类 → 其他 → 新增） |
| `status` | string | `success` / `fail` |

#### 4.7.3 `items` 数组元素字段

| 参数名 | 参数类型 | 说明 |
| --- | --- | --- |
| `name` | string | 分类名：`系统类` / `容器云` / `网络类` / `其他` / `新增` |
| `count` | number(int) | 该分类知识条目数（整数；区间：系统类 60~120、容器云 35~80、网络类 25~60、其他 15~45、新增 5~25） |

#### 4.7.4 响应示例

```json
{
  "total": 233,
  "items": [
    { "name": "系统类", "count": 78 },
    { "name": "容器云", "count": 58 },
    { "name": "网络类", "count": 42 },
    { "name": "其他", "count": 17 },
    { "name": "新增", "count": 18 }
  ],
  "status": "success"
}
```

#### 4.7.5 大屏用途

- 第 2 屏「知识库」卡：`total` → 主数值"条知识条目"，`items` 每项渲染一条内嵌占比条（条宽 = `count / total`，色板由前端按分类映射：系统类青/容器云紫/网络类绿/其他橙/新增红），悬停显示占比；
- 数据未拉回（接口失败前）该卡保持「暂无知识数据」空态动画。

---

## 5. 前端消费与轮询周期汇总

| 接口 | 前端触发点 | 周期 |
| --- | --- | --- |
| `/getMonitorAlertLevelData` | `fetchRealtimeData`（统计卡数值刷新）；`recordTrendSnapshot`（频次推移采样写入 5 分钟槽） | 30 秒 / 5 分钟 |
| `/getMonitorAlertStatusData` | `fetchRealtimeData`（闭环收敛环形图数据） | 30 秒 |
| `/getMonitorAlertStatisticData` | `fetchRealtimeData`（新增卡数值） | 30 秒 |
| `/getMonitorOrderData` | `fetchItmsData`（ITSM 卡/图表/走马灯） | 30 秒 |
| `/searchMonitorrData` | `fetchAlertDetail`（未结告警滚动轨道增量合并） | 30 秒 |
| `/getMonitorMatrixData` | `fetchMatrixData`（矩阵卡数据整组替换） | 30 秒 |
| `/getMonitorKnowledgeData` | `fetchKnowledgeData`（知识库卡占比条） | 30 秒 |

> 图表渲染：频次推移折线、闭环环形图、ITSM 三图均按 3 秒周期整组重绘（仅重绘不拉数据）；未结告警滚动轨道由 50ms 本地定时器驱动位移。

## 6. 异常响应示例

参数缺失（HTTP 400，如 `date` 为空）：

```json
{ "message": "日期参数不能为空", "status": "fail" }
```

服务端异常（HTTP 500，`/searchMonitorrData` 失败时携带业务数据）：

```json
{ "message": "请求处理失败", "status": "fail" }
```

```json
{ "data": [], "message": "请求处理失败", "status": "fail" }
```

## 7. 变更记录

| 版本 | 日期 | 变更说明 | 维护人 |
| --- | --- | --- | --- |
| v1.0 | 2026-09-06 | 初版：依据 FastAPI 服务（`app.py` / 8000 端口）整理 | 大屏开发组 |
| v2.0 | 2026-09-07 | 全量修订：接口迁移至 Flask 服务（`Monitorr.py` → `rolePermission.py` 注册，5000 端口）；接口路径统一为 `getMonitor*` 命名并与前端直连调用一致（含 `/searchMonitorrData`）；新增 `/getMonitorMatrixData`（矩阵卡数据接口化）与 `/getMonitorKnowledgeData`（知识库接入）；补充值类型约定（数值型/展示型）与前端轮询周期汇总 | 大屏开发组 |
